export enum Modo {
    presentador = 'presentador',
    reproductor = 'reproductor',
}
export enum MediaType {
    image = 'image',
    video = 'video',
    audio = 'audio',
    unknown = 'unknown',
}
